[1, 2, 3].forEach(function(item, index) {
  console.log(item, index);
});
const array1 = [1, 2, 3, 4];
// const doubleArray1 = array1.map(item => item * item);

//  Type 2 
 const doubleArray1 = array1.map(item => item * item);

console.log(doubleArray1);

// Type 3 
// Implementation of Filter Method 
